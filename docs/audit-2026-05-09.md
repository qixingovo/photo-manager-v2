# photo-manager-v2 安全审核报告

**日期**：2026-05-09
**审核人**：Claude Code

## 总览

| 类别 | 通过 | 警告 | 失败 |
|------|------|------|------|
| 安全漏洞 | 6 | 0 | 0 |
| 代码质量 | 4 | 0 | 0 |
| 数据库 | 5 | 0 | 0 |
| 配置 | 2 | 0 | 0 |

**状态：全部通过 ✅**

---

## 检查清单

### 1. 安全漏洞

#### ✅ XSS: innerHTML 用户数据转义
**位置**：`app.js`, `mobile-app.js`, `share.js`
**验证**：
- `app.js`：所有 `innerHTML` 中的用户可控数据均包裹 `escapeHtml()` — `cat.name`(l.3847), `photo.name`(l.3888,3890,2641), `child.name`(l.1266), `parent.name`(l.1218), `displayName`(l.1047), `catNames`(l.3882), `photo.storage_path`(l.3895), `photoUrl`(l.5717)
- `mobile-app.js`：`cat.name`(l.2231,3213), `photo.name`(l.2600,2641)
- `<option>` 标签中的 `cat.name` 为浏览器安全渲染（option 内容不执行 HTML）
- `share.js`：无未转义的 `innerHTML` 用户数据

#### ✅ 密码哈希
**位置**：`app.js:5367-5399`, `mobile-app.js:5796-5830`
**验证**：
- `sha256()` 使用 Web Crypto API `crypto.subtle.digest('SHA-256', msgBuffer)`
- `setIntimatePassword()` 调用 `sha256(password + APP_CONFIG.PEPPER)` 后存储
- `verifyIntimatePassword()` 兼容旧明文密码（长度!=64 → 直接比较 + 自动升级为 hash）
- 新密码使用 `sha256(input + APP_CONFIG.PEPPER) === stored` 验证

#### ✅ Supabase Auth 认证
**位置**：`app.js:181`, `mobile-app.js:513`
**验证**：
- 登录使用 `supabase.auth.signInWithPassword({ email, password })`
- Session 检查使用 `supabase.auth.getSession()`
- 登出使用 `supabase.auth.signOut()`
- 用户身份通过 profiles 表查询：`.from('profiles').select('username, role').eq('user_id', session.user.id)`
- 无 `authenticate_user` RPC 残余

#### ✅ RLS 策略 tightened
**位置**：`migrations/010_fix_rls.sql`
**验证**：
- 15 个策略从 `TO anon, authenticated` → `TO authenticated`
- `share_links` 保留 TO anon（分享功能需要公开访问）
- profiles 表正确配置：SELECT 允许所有 authenticated，UPDATE 仅允许自己
- 无 `TO anon, authenticated` 残余

#### ✅ 文件上传类型校验
**位置**：`app.js:2410-2416`, `mobile-app.js:1912-1918`
**验证**：
- MIME 类型白名单：`image/jpeg, image/png, image/gif, image/webp, image/heic, image/heif`
- 扩展名白名单：`.jpg, .jpeg, .png, .gif, .webp, .heic, .heif`
- 拒绝时显示提示并 `continue`

#### ✅ Session 管理清理
**位置**：`app.js`, `mobile-app.js`
**验证**：
- 无 `getStoredSession()` / `saveSession()` / `clearSession()` 残余引用
- 无 `AUTH_SESSION_KEY` 常量
- 无 localStorage session 存储

---

### 2. 代码质量

#### ✅ currentUser 声明
**位置**：`app.js:78`
**验证**：
- `let currentUser = null;` 已声明
- `checkLogin()` 中通过 `getUserFromSession()` 正确赋值
- `handleLogin()` 中通过 `getUserFromSession()` 正确赋值
- `handleLogout()` 中 `currentUser = null`

#### ✅ 无已删除函数残余引用
**验证**：
- 全局搜索 `getStoredSession|saveSession|clearSession|AUTH_SESSION_KEY` → 0 匹配
- 全局搜索 `authenticate_user` → 0 匹配

#### ✅ checkLogin 实现正确
**位置**：`app.js:135-160`, `mobile-app.js:481-503`
**验证**：
- `app.js`：`supabase.auth.getSession()` → profiles 查表 → 设 currentUser
- `mobile-app.js`：`client.auth.getSession()` → profiles 查表 → 设 currentUser
- profiles 查询失败时均调用 `signOut()`

#### ✅ handleLogout 实现正确
**位置**：`app.js:425-429`, `mobile-app.js:555-559`
**验证**：
- 设置 `currentUser = null`
- 调用 `supabase.auth.signOut()` / `client.auth.signOut()`
- 显示登录页面

---

### 3. 数据库

#### ✅ RLS 策略收紧完整
**验证**：010 migration 覆盖了以下 15 个策略：
```
milestones, app_settings, albums, album_photos,
mood_diary, daily_chatter, intimate_records, period_records,
couple_tasks, couple_checkins, rpg_progress,
drift_bottles, secret_notes, nudges, time_capsules
```
share_links 正确保留 TO anon。

#### ✅ profiles 表完整
**位置**：`migrations/010_fix_rls.sql:8-22`
**验证**：
- `CREATE TABLE IF NOT EXISTS profiles (user_id UUID PK, username TEXT UNIQUE, role TEXT, created_at)`
- `ALTER TABLE profiles ENABLE ROW LEVEL SECURITY`
- `profiles_select_auth` — `FOR SELECT TO authenticated USING (true)`
- `profiles_update_self` — `FOR UPDATE TO authenticated USING (auth.uid() = user_id)`

#### ✅ SECURITY DEFINER 函数存在
**位置**：`migrations/010_fix_rls.sql:48-101`
**验证**：
- `get_shared_album(share_code TEXT)` — `SECURITY DEFINER` + `SET search_path = ''`
- 处理：share_links 查找 → 过期检查 → 相册查找 → 照片获取
- 返回 JSONB 包含 album 和 photos

#### ✅ CHECK 约束完整
**位置**：`migrations/009_add_length_checks.sql`
**验证**：8 个 CHECK 约束覆盖所有文本字段长度限制。

#### ✅ Migration 编号连续
**验证**：001-010 全部存在，无缺失或重复。

---

### 4. 配置

#### ✅ config.example.js 字段完整
**位置**：`config.example.js`
**验证**：
- `SUPABASE_URL` ✅
- `SUPABASE_ANON_KEY` ✅
- `SUPABASE_STORAGE_URL` ✅
- `PEPPER` ✅
- `USER_EMAILS` ✅ (laoda / xiaodi)

#### ✅ 版本号已更新
**验证**：
- `index.html` — `app.js?v=53` ✅
- `index-mobile.html` — `mobile-app.js?v=72` ✅
- `share.html` — `share.js?v=16` (未变，本次未修改 share.html)

---

## 建议修复优先级

**无需修复 — 所有检查项通过** ✅

当前项目状态：
- Phase 1（紧急修复）全部完成
- Phase 2（Supabase Auth 迁移）全部完成
- 无安全漏洞
- 无代码质量缺陷
- 数据库结构完整且一致
- 配置完整

---

## Supabase SQL Editor — 直接复制粘贴执行

✅ 无需执行任何 SQL 命令

当前代码库已匹配所有 migration 文件。如果你尚未执行以下 migration，请按顺序复制到 Supabase SQL Editor 执行：

### Migration 009（如果尚未执行）

```sql
-- ============================================
-- 009: 添加内容长度 CHECK 约束
-- 在 Supabase SQL Editor 中执行
-- ============================================
ALTER TABLE mood_diary ADD CONSTRAINT chk_mood_content_length CHECK (char_length(content) <= 5000);
ALTER TABLE daily_chatter ADD CONSTRAINT chk_chatter_content_length CHECK (char_length(content) <= 2000);
ALTER TABLE secret_notes ADD CONSTRAINT chk_secret_content_length CHECK (char_length(content) <= 5000);
ALTER TABLE drift_bottles ADD CONSTRAINT chk_bottle_content_length CHECK (char_length(content) <= 2000);
ALTER TABLE time_capsules ADD CONSTRAINT chk_capsule_title_length CHECK (char_length(title) <= 200);
ALTER TABLE time_capsules ADD CONSTRAINT chk_capsule_content_length CHECK (char_length(content) <= 10000);
ALTER TABLE milestones ADD CONSTRAINT chk_milestone_title_length CHECK (char_length(title) <= 200);
ALTER TABLE albums ADD CONSTRAINT chk_album_name_length CHECK (char_length(name) <= 200);
```

### Migration 010（如果尚未执行）

```sql
-- ============================================
-- 010 Part A: 创建 profiles 表
-- ============================================
CREATE TABLE IF NOT EXISTS profiles (
    user_id UUID PRIMARY KEY REFERENCES auth.users(id) ON DELETE CASCADE,
    username TEXT UNIQUE NOT NULL,
    role TEXT NOT NULL DEFAULT 'xiaodi',
    created_at TIMESTAMPTZ DEFAULT now()
);

ALTER TABLE profiles ENABLE ROW LEVEL SECURITY;

CREATE POLICY "profiles_select_auth" ON profiles
    FOR SELECT TO authenticated USING (true);

CREATE POLICY "profiles_update_self" ON profiles
    FOR UPDATE TO authenticated USING (auth.uid() = user_id);

-- ============================================
-- 010 Part B: 收紧所有 RLS 策略 → TO authenticated
-- ============================================
ALTER POLICY "allow_all_milestones" ON milestones TO authenticated;
ALTER POLICY "allow_all_app_settings" ON app_settings TO authenticated;
ALTER POLICY "allow_all_albums" ON albums TO authenticated;
ALTER POLICY "allow_all_album_photos" ON album_photos TO authenticated;
ALTER POLICY "allow_all_mood_diary" ON mood_diary TO authenticated;
ALTER POLICY "allow_all_daily_chatter" ON daily_chatter TO authenticated;
ALTER POLICY "allow_all_intimate_records" ON intimate_records TO authenticated;
ALTER POLICY "allow_all_period_records" ON period_records TO authenticated;
ALTER POLICY "allow_all_couple_tasks" ON couple_tasks TO authenticated;
ALTER POLICY "allow_all_couple_checkins" ON couple_checkins TO authenticated;
ALTER POLICY "allow_all_rpg_progress" ON rpg_progress TO authenticated;
ALTER POLICY "allow_all_drift_bottles" ON drift_bottles TO authenticated;
ALTER POLICY "allow_all_secret_notes" ON secret_notes TO authenticated;
ALTER POLICY "allow_all_nudges" ON nudges TO authenticated;
ALTER POLICY "allow_all_time_capsules" ON time_capsules TO authenticated;

-- ============================================
-- 010 Part C: 分享功能 SECURITY DEFINER 函数
-- ============================================
CREATE OR REPLACE FUNCTION get_shared_album(share_code TEXT)
RETURNS JSONB
LANGUAGE plpgsql
SECURITY DEFINER
SET search_path = ''
AS $$
DECLARE
    share_record RECORD;
    album_record RECORD;
    photo_ids BIGINT[];
    photos_json JSONB;
BEGIN
    SELECT * INTO share_record FROM share_links WHERE token = share_code;
    IF NOT FOUND THEN
        RETURN jsonb_build_object('error', '链接无效');
    END IF;

    IF share_record.expires_at IS NOT NULL AND share_record.expires_at < now() THEN
        RETURN jsonb_build_object('error', '链接已过期');
    END IF;

    SELECT * INTO album_record FROM albums WHERE id = share_record.album_id;
    IF NOT FOUND THEN
        RETURN jsonb_build_object('error', '相册不存在');
    END IF;

    SELECT array_agg(photo_id) INTO photo_ids FROM album_photos WHERE album_id = share_record.album_id;

    IF photo_ids IS NOT NULL AND array_length(photo_ids, 1) > 0 THEN
        SELECT jsonb_agg(
            jsonb_build_object(
                'id', p.id,
                'name', p.name,
                'description', p.description,
                'storage_path', p.storage_path,
                'location_name', p.location_name,
                'is_favorite', p.is_favorite,
                'created_at', p.created_at
            )
            ORDER BY p.created_at DESC
        ) INTO photos_json
        FROM photos p
        WHERE p.id = ANY(photo_ids);
    ELSE
        photos_json := '[]'::jsonb;
    END IF;

    RETURN jsonb_build_object(
        'album', jsonb_build_object(
            'id', album_record.id,
            'name', album_record.name,
            'description', album_record.description
        ),
        'photos', COALESCE(photos_json, '[]'::jsonb)
    );
END;
$$;
```
